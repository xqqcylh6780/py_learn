[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-engineering-app"
version = "0.1.0"
description = "project_engineering_study sample project"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
demo-engineering = "demo_app.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
