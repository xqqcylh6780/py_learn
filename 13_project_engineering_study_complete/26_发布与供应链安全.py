# -*- coding: utf-8 -*-
"""
26 发布与供应链安全
===========

发布从干净构建、测试和可信凭据管理开始，不把长期 token 写进仓库或 CI 日志。

发布链的每一步都应能追溯到源码提交、构建环境、产物摘要和批准记录。
短期身份和最小权限可以缩小凭据泄露后的影响，受保护 tag 可防止构建来源被替换。
依赖扫描只是辅助，仍需审查产物内容并在隔离环境验证实际安装结果。
"""

print('典型发布链: tag/release -> CI test -> build -> inspect -> publish -> smoke test')
print('\n安全重点:')
for x in ['启用 MFA','优先短期/可信发布身份','保护发布分支和 tag','检查构建产物内容','依赖更新走审核与测试']:
    print('-',x)
print('发布前先在隔离环境安装 wheel 做 smoke test。')
