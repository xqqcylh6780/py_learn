# -*- coding: utf-8 -*-
"""
25 本地开发工作流
==========

稳定工作流应是可重复命令序列，而不是依赖 IDE 中某个人的隐藏设置。

新成员和 CI 应能从文档中的少量命令得到相同结果，环境准备步骤也应可重新执行。
把格式化、测试、构建等命令集中到明确入口，可以减少个人工具配置造成的偏差。
工作流发生变化时要同步更新文档和 CI，否则本地成功不能代表合并后成功。
"""

steps=[
'1. 获取源码','2. 选择受支持 Python','3. 创建 .venv','4. 安装项目/开发依赖',
'5. 测试/类型/lint','6. 开发','7. 再跑质量门禁','8. build','9. 干净环境安装验证']
print('\n'.join(steps))
print('\n把关键命令写进 README、脚本或 task runner。')
